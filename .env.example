# Django
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_DEBUG=0
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# URLs internas (comunicación entre servicios)
# Local: cada proceso en tu máquina
# Docker: usar nombres de servicio (django, fastapi)
DJANGO_INTERNAL_URL=http://127.0.0.1:8000
FASTAPI_INTERNAL_URL=http://127.0.0.1:8001

# Base de datos (PostgreSQL)
DB_NAME=safelease
DB_USER=safelease
DB_PASSWORD=safelease
DB_HOST=127.0.0.1
DB_PORT=5432

# FastAPI
FASTAPI_DEBUG=0
